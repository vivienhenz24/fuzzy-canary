{
  "name": "@fuzzycanary/vite",
  "version": "0.1.0",
  "description": "Vite plugin adapter for @fuzzycanary/core. Injects init() during build.",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "vite": ">=4.0.0",
    "@fuzzycanary/core": "0.1.0"
  },
  "devDependencies": {
    "vite": "^5.4.8",
    "@fuzzycanary/core": "^0.1.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
