{
  "_comment": "TypeScript configuration for the core package - extends root config and specifies compilation options for building the SDK",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*"]
}
